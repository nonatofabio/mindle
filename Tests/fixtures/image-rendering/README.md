# Local image fixture

This fixture verifies that Mindle rewrites a relative image to its local
`mindle-file://` URL and that the custom scheme handler returns image bytes.

![Mindle local fixture](images/sample.svg)
